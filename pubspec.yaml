name: flutter_chat_starter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1  # Add this
  
  # Firebase Core & Auth
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  
  # Database & Storage
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0

  firebase_messaging: ^16.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true